#include <bits/stdc++.h>
using namespace std;

unsigned long long main() {
    unsigned long long a; 
    cin >> a;
    unsigned long long f = (a * 9 / 5) + 32;

    cout << fixed << setprecision(1) << f << endl;
    return 0;
}